  FROM EMPLOYEES;</code></pre>
<pre><button class="copy">복사</button><code>SELECT DISTINCT JOB_ID, DEPARTMENT_ID
  FROM EMPLOYEES;</code></pre>
<div class="callout">컬럼이 2개면 두 컬럼 <b>조합의 중복</b>을 제거합니다.</div>
</div></section><section id="s4" data-subject="select">
<div class="section-title" onclick="toggleSection(this)"><h2>4. 별칭 + 산술</h2><span class="badge">AS / + - * /</span></div>
<div class="section-body">
<table><tr><th>기능</th><th>예</th></tr>
<tr><td>별칭</td><td><code>SALARY AS "급여"</code></td></tr>
<tr><td>연봉</td><td><code>SALARY * 12</code></td></tr>
<tr><td>날짜 + 일수</td><td><code>HIRE_DATE + 1</code></td></tr></table>
<pre><button class="copy">복사</button><code>SELECT FIRST_NAME, SALARY,
       SALARY * 12 AS "연봉"
  FROM EMPLOYEES;</code></pre>
</div></section><section id="s5" data-subject="select">
<div class="section-title" onclick="toggleSection(this)"><h2>5. NULL 기본</h2><span class="badge">0·공백과 다름</span></div>
<div class="section-body">
<div class="danger"><b>NULL</b>은 값이 비어 있거나 정의되지 않은 상태입니다. 0이나 공백과 다르고, NULL과 산술 연산하면 결과도 NULL이 됩니다.</div>
</div></section><section id="s6" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>6. WHERE</h2><span class="badge">조건 지정</span></div>
<div class="section-body">
<pre><button class="copy">복사</button><code>SELECT *
  FROM EMPLOYEES
 WHERE JOB_ID = 'IT_PROG';</code></pre>
<div class="warn"><b>SQL에서 같다:</b> <code>=</code> 하나 사용. 문자값은 작은따옴표로 묶습니다.</div>
</div></section><section id="s7" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>7. 비교 연산자</h2><span class="badge">= / <> / >= / <=</span></div>
<div class="section-body">
<table><tr><th>의미</th><th>기호</th></tr>
<tr><td>같다</td><td><code>=</code></td></tr>
<tr><td>같지 않다</td><td><code>!=</code>, <code>&lt;&gt;</code>, <code>^=</code>, <code>NOT =</code></td></tr>
<tr><td>크다/이상</td><td><code>&gt;</code>, <code>&gt;=</code></td></tr>
<tr><td>작다/이하</td><td><code>&lt;</code>, <code>&lt;=</code></td></tr></table>
</div></section><section id="s8" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>8. AND / OR</h2><span class="badge">AND 우선</span></div>
<div class="section-body">
<pre><button class="copy">복사</button><code>SELECT EMPLOYEE_ID, FIRST_NAME
  FROM EMPLOYEES
 WHERE DEPARTMENT_ID = 90
   AND SALARY &gt;= 5000;</code></pre>
<div class="danger"><b>우선순위:</b> AND가 OR보다 먼저. OR을 먼저 계산하려면 소괄호로 묶습니다.</div>
</div></section><section id="s9" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>9. IS NULL</h2><span class="badge">NULL 전용 비교</span></div>
<div class="section-body">
<table><tr><th>목적</th><th>문법</th></tr>
<tr><td>NULL</td><td><code>IS NULL</code></td></tr>
<tr><td>NULL 아님</td><td><code>IS NOT NULL</code></td></tr></table>
<pre><button class="copy">복사</button><code>SELECT FIRST_NAME, PHONE_NUMBER
  FROM EMPLOYEES
 WHERE PHONE_NUMBER IS NULL;</code></pre>
<div class="danger"><code>= NULL</code>, <code>!= NULL</code>, <code>&lt;&gt; NULL</code> 사용 금지.</div>
</div></section><section id="s10" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>10. IN / NOT IN</h2><span class="badge">여러 값 포함·제외</span></div>
<div class="section-body">
<table><tr><th>목적</th><th>문법</th></tr>
<tr><td>여러 값 중 하나</td><td><code>IN (30, 50, 90)</code></td></tr>
<tr><td>여러 값 제외</td><td><code>NOT IN (30, 50, 90)</code></td></tr></table>
<pre><button class="copy">복사</button><code>SELECT *
  FROM EMPLOYEES
 WHERE DEPARTMENT_ID IN (30, 50, 90);</code></pre>
<div class="danger">수업에서는 <b>NOT IN + NULL</b>을 중요한 주의점으로 다뤘습니다.</div>
</div></section><section id="s11" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>11. BETWEEN</h2><span class="badge">범위</span></div>
<div class="section-body">
<pre><button class="copy">복사</button><code>SELECT FIRST_NAME, SALARY
  FROM EMPLOYEES
 WHERE SALARY BETWEEN 10000 AND 19999;</code></pre>
</div></section><section id="like" data-subject="where">
<div class="section-title" onclick="toggleSection(this)"><h2>12. LIKE</h2><span class="badge">% / _</span></div>
<div class="section-body">
<table><tr><th>찾기</th><th>패턴</th></tr>
<tr><td>S로 시작</td><td><code>LIKE 'S%'</code></td></tr>
<tr><td>s로 끝</td><td><code>LIKE '%s'</code></td></tr>
<tr><td>s 포함</td><td><code>LIKE '%s%'</code></td></tr>
<tr><td>S로 시작하는 3글자</td><td><code>LIKE 'S__'</code></td></tr></table>
<div class="callout"><code>%</code>는 여러 문자, <code>_</code>는 한 글자 자리를 나타냅니다.</div>
</div></section><section id="charfunc" data-subject="func">
<div class="section-title" onclick="toggleSection(this)"><h2>13. 문자 함수</h2><span class="badge">UPPER / LOWER / LENGTH / SUBSTR / REPLACE / CONCAT / TRIM</span></div>
<div class="section-body">
<table><tr><th>함수</th><th>기능</th><th>형태</th></tr>
<tr><td>UPPER</td><td>대문자</td><td><code>UPPER(FIRST_NAME)</code></td></tr>
<tr><td>LOWER</td><td>소문자</td><td><code>LOWER(FIRST_NAME)</code></td></tr>
<tr><td>LENGTH</td><td>길이</td><td><code>LENGTH(FIRST_NAME)</code></td></tr>
<tr><td>SUBSTR</td><td>일부 추출</td><td><code>SUBSTR(문자, 시작, 길이)</code></td></tr>
<tr><td>REPLACE</td><td>치환</td><td><code>REPLACE(문자, 기존, 새문자)</code></td></tr>
<tr><td>CONCAT</td><td>연결</td><td><code>CONCAT(문자1, 문자2)</code></td></tr>
<tr><td>TRIM</td><td>공백 제거</td><td><code>TRIM(문자)</code></td></tr></table>
</div></section><section id="s14" data-subject="func">
<div class="section-title" onclick="toggleSection(this)"><h2>14. 숫자 함수</h2><span class="badge">MOD / ROUND / TRUNC</span></div>
<div class="section-body">
<table><tr><th>함수</th><th>기능</th></tr><tr><td>MOD</td><td>나머지</td></tr><tr><td>ROUND</td><td>반올림</td></tr><tr><td>TRUNC</td><td>버림</td></tr></table>
<pre><button class="copy">복사</button><code>SELECT MOD(10,3),
       ROUND(123.456,2),
       TRUNC(123.456,2)
  FROM DUAL;</code></pre>
</div></section><section id="s15" data-subject="func">
<div class="section-title" onclick="toggleSection(this)"><h2>15. 날짜 함수</h2><span class="badge">SYSDATE / ADD_MONTHS</span></div>
<div class="section-body">
<table><tr><th>사용</th><th>기능</th></tr>
<tr><td><code>SYSDATE</code></td><td>현재 날짜</td></tr>
<tr><td><code>SYSDATE + 1</code></td><td>1일 더하기</td></tr>
<tr><td><code>ADD_MONTHS(날짜, 개월)</code></td><td>개월 더하기</td></tr></table>
</div></section><section id="s16" data-subject="func">
<div class="section-title" onclick="toggleSection(this)"><h2>16. 형변환 함수</h2><span class="badge">TO_CHAR / TO_NUMBER / TO_DATE</span></div>
<div class="section-body">
<table><tr><th>함수</th><th>변환</th></tr>
<tr><td>TO_CHAR</td><td>숫자/날짜 → 문자</td></tr>
<tr><td>TO_NUMBER</td><td>문자 → 숫자</td></tr>
<tr><td>TO_DATE</td><td>문자 → 날짜</td></tr></table>
<div class="callout">수업 필기에서는 자료형 우선순위를 <b>날짜 &gt; 숫자 &gt; 문자</b>로 정리했습니다.</div>
</div></section><section id="s17" data-subject="func">
<div class="section-title" onclick="toggleSection(this)"><h2>17. NULL 함수</h2><span class="badge">NVL / NVL2</span></div>
<div class="section-body">
<table><tr><th>함수</th><th>기능</th><th>형태</th></tr>
<tr><td>NVL</td>